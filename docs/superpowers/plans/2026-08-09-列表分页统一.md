# 列表分页统一（方案 A）Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 将 `GET /user/page` 分页参数统一为 `pageNum/pageSize`、新增 `authStatus` DB 级过滤（消除逐行 `selectById` 的 N+1），并让 RealNameAudit 改服务端分页、UserList 分页参数对齐。

**Architecture:** 后端在 `UsersMapper` 加注解 `@Select` JOIN 分页查询（先按条件过滤出 userId 集合 + count），Service 再用 `selectBatchIds` 批量装载 users/usersinfo 组装 `UserPageVO`，消除 N+1。前端 UserList 仅改参数名；RealNameAudit 由「`size:200` 全量加载 + 客户端过滤」改为「`pageNum/pageSize/authStatus` 服务端查询 + el-pagination + 统计卡轻量计数」。

**Tech Stack:** Spring Boot 2.7.3 / MyBatis-Plus 3.4.3 / MySQL 8 / Vue 2 / Element UI

**验证约定（适配本项目）**：本项目无活跃单元测试基建，且 Coding-Agent 角色（CLAUDE.md + 启动提示词）限定验证为 `mvn compile`（后端）与 `npm run build`（前端），功能验收由 Business Validator 用例执行。故各任务以构建校验为门禁，末尾有验证器契约同步任务。

**前置（git 状态）**：当前工作树有未提交的 D-01~D-10 修复与 RealNameAudit 改版。建议执行本计划前先一次性提交这些既有改动，使各任务的 commit 只含各自文件。

---

### Task 1: UsersConfig DTO — pageNum/pageSize + authStatus 过滤字段

**Files:**
- Modify: `src/main/java/com/lz/pojo/Page/UsersConfig.java`

- [ ] **Step 1: 替换文件内容**

```java
package com.lz.pojo.Page;

import lombok.Builder;
import lombok.Data;

/**
 * 用户配置
 *
 * @author lz
 * @date 2024/04/15
 */
@Data
@Builder
public class UsersConfig {
    private String username;
    private String email;
    private Boolean isActive;
    private int pageNum;
    private int pageSize;
    /** 认证状态中文值（认证中/认证通过/认证失败/未认证），null=不过滤 */
    private String authStatus;
    /** 认证状态 dbValue（由 authStatus 在 Service 映射），null=不过滤 */
    private Integer authStatusDb;
    /** 未认证（无 usersinfo 记录）过滤开关 */
    private boolean unauthenticatedOnly;
    /** 分页偏移量（Service 计算，供 JOIN 查询 LIMIT 使用） */
    private int offset;
}
```

- [ ] **Step 2: 后端编译校验**

Run: `mvn compile -q -DskipTests`
Expected: 退出码 0，无输出。

- [ ] **Step 3: Commit**

```bash
git add src/main/java/com/lz/pojo/Page/UsersConfig.java
git commit -m "feat: UsersConfig 分页参数对齐 pageNum/pageSize 并加 authStatus 过滤字段"
```

---

### Task 2: AuthenticationStatus — 增加 fromDescription 中文值映射

**Files:**
- Modify: `src/main/java/com/lz/pojo/Enum/AuthenticationStatus.java`

- [ ] **Step 1: 在枚举类末尾（`fromDbValue` 方法之后、类结束大括号之前）新增静态方法**

```java
    public static AuthenticationStatus fromDescription(String description) {
        for (AuthenticationStatus status : AuthenticationStatus.values()) {
            if (status.getDescription().equals(description)) {
                return status;
            }
        }
        return null;
    }
```

- [ ] **Step 2: 后端编译校验**

Run: `mvn compile -q -DskipTests`
Expected: 退出码 0，无输出。

- [ ] **Step 3: Commit**

```bash
git add src/main/java/com/lz/pojo/Enum/AuthenticationStatus.java
git commit -m "feat: AuthenticationStatus 增加 fromDescription 中文描述映射"
```

---

### Task 3: UsersMapper — JOIN 分页查询方法

**Files:**
- Modify: `src/main/java/com/lz/mapper/UsersMapper.java`

- [ ] **Step 1: 替换文件内容**

```java
package com.lz.mapper;

import com.lz.pojo.entity.Users;
import com.baomidou.mybatisplus.core.mapper.BaseMapper;
import com.lz.pojo.Page.UsersConfig;
import org.apache.ibatis.annotations.Mapper;
import org.apache.ibatis.annotations.Select;

import java.util.List;

/**
 * <p>
 * 存储系统用户信息 Mapper 接口
 * </p>
 *
 * @author lz
 * @since 2024-04-04
 */
@Mapper
public interface UsersMapper extends BaseMapper<Users> {

    /**
     * 按用户名获取
     *
     * @param username 用户名
     *
     * @return {@code Users}
     */
    Users getByUsername(String username);

    /**
     * 按条件（username/email/isActive/authStatus）JOIN usersinfo 过滤并分页返回 userId
     *
     * @param config 查询条件（含 pageSize/offset）
     *
     * @return {@code List<Long>}
     */
    @Select("<script>"
            + "SELECT u.UserID FROM users u LEFT JOIN usersinfo ui ON ui.UserID = u.UserID "
            + "<where>"
            + "<if test='username != null and username.trim() != \"\"'> AND u.Username LIKE CONCAT('%', #{username}, '%')</if>"
            + "<if test='email != null and email.trim() != \"\"'> AND u.Email LIKE CONCAT('%', #{email}, '%')</if>"
            + "<if test='isActive != null'> AND u.IsActive = #{isActive}</if>"
            + "<if test='authStatusDb != null'> AND ui.AuthStatus = #{authStatusDb}</if>"
            + "<if test='unauthenticatedOnly'> AND ui.UserID IS NULL</if>"
            + "</where>"
            + "ORDER BY u.UserID "
            + "LIMIT #{pageSize} OFFSET #{offset}"
            + "</script>")
    List<Long> selectFilteredUserIds(UsersConfig config);

    /**
     * 同条件 count（供分页 total 使用）
     *
     * @param config 查询条件
     *
     * @return {@code Long}
     */
    @Select("<script>"
            + "SELECT COUNT(*) FROM users u LEFT JOIN usersinfo ui ON ui.UserID = u.UserID "
            + "<where>"
            + "<if test='username != null and username.trim() != \"\"'> AND u.Username LIKE CONCAT('%', #{username}, '%')</if>"
            + "<if test='email != null and email.trim() != \"\"'> AND u.Email LIKE CONCAT('%', #{email}, '%')</if>"
            + "<if test='isActive != null'> AND u.IsActive = #{isActive}</if>"
            + "<if test='authStatusDb != null'> AND ui.AuthStatus = #{authStatusDb}</if>"
            + "<if test='unauthenticatedOnly'> AND ui.UserID IS NULL</if>"
            + "</where>"
            + "</script>")
    Long countFilteredUsers(UsersConfig config);
}
```

说明：`auth_status` 列为 varchar，`#{authStatusDb}` 传 Integer，MySQL 隐式转换匹配；`未认证` 用 `ui.UserID IS NULL`（LEFT JOIN 无记录）而非 auth_status=0。

- [ ] **Step 2: 后端编译校验**

Run: `mvn compile -q -DskipTests`
Expected: 退出码 0，无输出。

- [ ] **Step 3: Commit**

```bash
git add src/main/java/com/lz/mapper/UsersMapper.java
git commit -m "feat: UsersMapper JOIN usersinfo 分页过滤查询与 count"
```

---

### Task 4: UsersServiceImpl.getUserByPage — JOIN 过滤 + 批量装载 + authStatus 映射

**Files:**
- Modify: `src/main/java/com/lz/service/impl/UsersServiceImpl.java`（imports + 替换 getUserByPage 方法 + 新增 applyAuthStatusFilter）

- [ ] **Step 1: 在 imports 区（`import java.util.List;` 之后）新增**

```java
import java.util.Map;
```

- [ ] **Step 2: 整体替换 `getUserByPage` 方法（当前第 115-157 行）并新增 `applyAuthStatusFilter` 私有方法**

```java
    public PageResult getUserByPage(UsersConfig config) {
        applyAuthStatusFilter(config);
        long total = usersMapper.countFilteredUsers(config);
        List<UserPageVO> userPageVOS = new ArrayList<>();
        if (total > 0) {
            config.setOffset((config.getPageNum() - 1) * config.getPageSize());
            List<Long> userIds = usersMapper.selectFilteredUserIds(config);
            if (!userIds.isEmpty()) {
                Map<Long, Users> userMap = usersMapper.selectBatchIds(userIds).stream()
                        .collect(Collectors.toMap(Users::getUserId, u -> u, (a, b) -> a));
                Map<Long, UsersInfo> infoMap = usersInfoMapper.selectBatchIds(userIds).stream()
                        .collect(Collectors.toMap(UsersInfo::getUserId, ui -> ui, (a, b) -> a));
                for (Long userId : userIds) {
                    Users users = userMap.get(userId);
                    if (users == null) {
                        continue;
                    }
                    UserPageVO userPageVO = new UserPageVO();
                    BeanUtils.copyProperties(users, userPageVO);
                    UsersInfo byId = infoMap.get(userId);
                    if (byId == null) {
                        userPageVO.setAuthStatus(AuthenticationStatus.UNAUTHORIZED);
                        userPageVO.setAuthLevel(0);
                    } else {
                        userPageVO.setAuthStatus(byId.getAuthStatus());
                        userPageVO.setAuthLevel(byId.getAuthLevel());
                        userPageVO.setIdentityNo(byId.getIdentityNo());
                        userPageVO.setName(byId.getName());
                        userPageVO.setUserRole(byId.getUserRole());
                        userPageVO.setRoleImgSrc(byId.getRoleImgSrc());
                        userPageVO.setCertifieTime(byId.getCertifieTime());
                    }
                    userPageVO.setCreditScore(users.getCreditScore());
                    userPageVOS.add(userPageVO);
                }
            }
        }
        return new PageResult<>(total, userPageVOS);
    }

    private void applyAuthStatusFilter(UsersConfig config) {
        config.setAuthStatusDb(null);
        config.setUnauthenticatedOnly(false);
        String authStatus = config.getAuthStatus();
        if (authStatus == null || authStatus.trim().isEmpty()) {
            return;
        }
        if ("未认证".equals(authStatus)) {
            config.setUnauthenticatedOnly(true);
            return;
        }
        AuthenticationStatus status = AuthenticationStatus.fromDescription(authStatus);
        if (status != null) {
            config.setAuthStatusDb(status.getDbValue());
        }
    }
```

注意：替换后方法体内不再使用 `Page`/`QueryWrapper`（import 保留不删，其余方法仍用到）。

- [ ] **Step 3: 后端编译校验**

Run: `mvn compile -q -DskipTests`
Expected: 退出码 0，无输出。

- [ ] **Step 4: Commit**

```bash
git add src/main/java/com/lz/service/impl/UsersServiceImpl.java
git commit -m "feat: getUserByPage 改 JOIN 过滤 + 批量装载消 N+1，支持 authStatus 过滤"
```

---

### Task 5: UsersController — /user/page 参数对齐 + authStatus

**Files:**
- Modify: `src/main/java/com/lz/controller/UsersController.java`（`getUserInfoByPage`，当前第 243-259 行）

- [ ] **Step 1: 整体替换 `getUserInfoByPage` 方法**

```java
    @GetMapping(value = "/page")
    @ApiOperation("分页查询用户信息")
    public Result<PageResult> getUserInfoByPage(@RequestParam(required = false) String username,
            @RequestParam(required = false) String email,
            @RequestParam(required = false) String isActive,
            @RequestParam(required = false) String authStatus,
            @RequestParam(defaultValue = "1") int pageNum,
            @RequestParam(defaultValue = "10") int pageSize) {
        log.info("分页查询用户信息:{}", isActive);
        Boolean is = null;
        if (isActive != null && !"".equals(isActive)) {
            is = "TRUE".equals(isActive);
        }
        UsersConfig config = UsersConfig.builder().username(username).email(email).isActive(is)
                .authStatus(authStatus).pageNum(pageNum).pageSize(pageSize).build();
        log.info("分页查询用户信息:{}", config);
        return Result.success(usersService.getUserByPage(config));
    }
```

- [ ] **Step 2: 后端编译校验**

Run: `mvn compile -q -DskipTests`
Expected: 退出码 0，无输出。

- [ ] **Step 3: Commit**

```bash
git add src/main/java/com/lz/controller/UsersController.java
git commit -m "feat: /user/page 参数 page/size 对齐为 pageNum/pageSize 并新增 authStatus"
```

---

### Task 6: 后端集成验证 + 契约确认

- [ ] **Step 1: 全量编译**

Run: `mvn compile -q -DskipTests`
Expected: 退出码 0。

- [ ] **Step 2: 确认契约变更已生效（供转达验证器）**

- `GET /user/page`：`page/size` → `pageNum/pageSize`；新增可选 `authStatus`（中文值：认证中/认证通过/认证失败/未认证）。
- `未认证` 语义：无 `usersinfo` 记录（`ui.UserID IS NULL`）。

---

### Task 7: UserList.vue — 分页参数对齐

**Files:**
- Modify: `web/src/views/admin/UserList.vue`

- [ ] **Step 1: `data()` 中 `userInfoConfig`（当前第 223-229 行）改字段名**

把：
```js
                userInfoConfig: {
                    username: "",
                    email: "",
                    isActive: "",
                    size: 5,
                    page: 1
                },
```
改为：
```js
                userInfoConfig: {
                    username: "",
                    email: "",
                    isActive: "",
                    pageSize: 5,
                    pageNum: 1
                },
```

- [ ] **Step 2: 模板 `el-pagination`（当前第 144-147 行）绑定改字段名**

把：
```html
                <el-pagination @size-change="handleSizeChange" @current-change="handleCurrentChange"
                    :hide-on-single-page="hide_on_single_page" :current-page=userInfoConfig.page
                    :page-sizes="[5, 7, 10, 15]" :page-size=userInfoConfig.size
                    layout="total, sizes, prev, pager, next, jumper" :total="total">
```
改为：
```html
                <el-pagination @size-change="handleSizeChange" @current-change="handleCurrentChange"
                    :hide-on-single-page="hide_on_single_page" :current-page=userInfoConfig.pageNum
                    :page-sizes="[5, 7, 10, 15]" :page-size=userInfoConfig.pageSize
                    layout="total, sizes, prev, pager, next, jumper" :total="total">
```

- [ ] **Step 3: `handleSizeChange`（当前第 338-342 行）改字段名**

把 `this.userInfoConfig.size = val` 改为 `this.userInfoConfig.pageSize = val`。

- [ ] **Step 4: `handleCurrentChange`（当前第 343-347 行）改字段名**

把 `this.userInfoConfig.page = val` 改为 `this.userInfoConfig.pageNum = val`。

- [ ] **Step 5: 前端构建校验**

Run: `(cd web && npm run build) 2>&1 | tail -8`
Expected: 结尾含 `DONE  Build complete.`

- [ ] **Step 6: Commit**

```bash
git add web/src/views/admin/UserList.vue
git commit -m "feat: UserList 分页参数 page/size 对齐为 pageNum/pageSize"
```

---

### Task 8: RealNameAudit.vue — 服务端分页

**Files:**
- Modify: `web/src/views/admin/RealNameAudit.vue`

- [ ] **Step 1: 模板：表格绑定改 `records`，表格卡片后新增分页**

把模板中 `:data="filteredList"` 改为 `:data="records"`；在 `</el-table>` 之后、`</div>`（rna-table-card 收尾）之前插入：

```html
                <el-pagination class="rna-pagination" background layout="total, sizes, prev, pager, next, jumper"
                    :total="total" :current-page="pageNum" :page-size="pageSize" :page-sizes="[10, 20, 50]"
                    @size-change="handleSizeChange" @current-change="handleCurrentChange">
                </el-pagination>
```

- [ ] **Step 2: 脚本整体替换 `data()`、`computed`、`methods` 中与加载/过滤/统计相关的部分**

把：
```js
        data() {
            return {
                activeTab: '认证中',
                allUsers: [],
                loading: false,
                statCards: [
                    { name: '认证中', label: '待审核', cls: 'rna-st-warn', icon: 'el-icon-time' },
                    { name: '认证通过', label: '已通过', cls: 'rna-st-ok', icon: 'el-icon-circle-check' },
                    { name: '认证失败', label: '已驳回', cls: 'rna-st-fail', icon: 'el-icon-circle-close' },
                    { name: 'ALL', label: '全部', cls: 'rna-st-all', icon: 'el-icon-tickets' }
                ]
            }
        },
        computed: {
            filteredList() {
                if (this.activeTab === 'ALL') return this.allUsers
                return this.allUsers.filter(u => u.authStatus === this.activeTab)
            }
        },
```
改为：
```js
        data() {
            return {
                activeTab: '认证中',
                records: [],
                total: 0,
                pageNum: 1,
                pageSize: 10,
                loading: false,
                statCounts: { '认证中': 0, '认证通过': 0, '认证失败': 0, ALL: 0 },
                statCards: [
                    { name: '认证中', label: '待审核', cls: 'rna-st-warn', icon: 'el-icon-time' },
                    { name: '认证通过', label: '已通过', cls: 'rna-st-ok', icon: 'el-icon-circle-check' },
                    { name: '认证失败', label: '已驳回', cls: 'rna-st-fail', icon: 'el-icon-circle-close' },
                    { name: 'ALL', label: '全部', cls: 'rna-st-all', icon: 'el-icon-tickets' }
                ]
            }
        },
```

- [ ] **Step 3: 替换 `loadData` 与 `switchTab`，新增 `loadStatCounts/countByStatus/handleSizeChange/handleCurrentChange`，并把 `statCount` 改为读 `statCounts`**

把：
```js
            loadData() {
                this.loading = true
                getUserList({ page: 1, size: 200 }).then(res => {
                    if (res.data.code === SUCCESS_CODE) {
                        this.allUsers = res.data.data.records || []
                    }
                    this.loading = false
                }).catch(err => {
                    console.error('获取实名申请失败：', err)
                    this.$message.error('请求异常，请稍后重试')
                    this.loading = false
                })
            },
            switchTab(name) {
                if (this.activeTab !== name) {
                    this.activeTab = name
                    this.loadData()
                }
            },
            statCount(name) {
                if (name === 'ALL') return this.allUsers.length
                return this.allUsers.filter(u => u.authStatus === name).length
            },
```
改为：
```js
            loadData() {
                this.loading = true
                const authStatus = this.activeTab === 'ALL' ? undefined : this.activeTab
                getUserList({ pageNum: this.pageNum, pageSize: this.pageSize, authStatus }).then(res => {
                    if (res.data.code === SUCCESS_CODE) {
                        this.records = res.data.data.records || []
                        this.total = res.data.data.total || 0
                    }
                    this.loading = false
                }).catch(err => {
                    console.error('获取实名申请失败：', err)
                    this.$message.error('请求异常，请稍后重试')
                    this.loading = false
                })
                this.loadStatCounts()
            },
            loadStatCounts() {
                const tabs = ['认证中', '认证通过', '认证失败']
                Promise.all(tabs.map(t => this.countByStatus(t))).then(([a, b, c]) => {
                    this.statCounts['认证中'] = a
                    this.statCounts['认证通过'] = b
                    this.statCounts['认证失败'] = c
                })
                this.countByStatus(undefined).then(n => { this.statCounts.ALL = n })
            },
            countByStatus(authStatus) {
                return getUserList({ pageNum: 1, pageSize: 1, authStatus }).then(res =>
                    res.data.code === SUCCESS_CODE ? (res.data.data.total || 0) : 0
                ).catch(() => 0)
            },
            switchTab(name) {
                if (this.activeTab !== name) {
                    this.activeTab = name
                    this.pageNum = 1
                    this.loadData()
                }
            },
            statCount(name) {
                if (name === 'ALL') return this.statCounts.ALL
                return this.statCounts[name] || 0
            },
            handleSizeChange(size) {
                this.pageSize = size
                this.pageNum = 1
                this.loadData()
            },
            handleCurrentChange(page) {
                this.pageNum = page
                this.loadData()
            },
```

- [ ] **Step 4: 样式新增分页条样式（`<style scoped>` 内，`.rna-empty-wrap` 之后）**

```css
    .rna-pagination {
        display: flex;
        justify-content: flex-end;
        margin-top: 14px;
    }
```

- [ ] **Step 5: 前端构建校验**

Run: `(cd web && npm run build) 2>&1 | tail -8`
Expected: 结尾含 `DONE  Build complete.`

- [ ] **Step 6: Commit**

```bash
git add web/src/views/admin/RealNameAudit.vue
git commit -m "feat: RealNameAudit 改服务端分页（pageNum/pageSize/authStatus + el-pagination + 统计卡计数）"
```

---

### Task 9: 前端集成验证

- [ ] **Step 1: 全量构建**

Run: `(cd web && npm run build) 2>&1 | tail -8`
Expected: 结尾含 `DONE  Build complete.`

- [ ] **Step 2: 后端全量编译复跑**

Run: `mvn compile -q -DskipTests`
Expected: 退出码 0。

---

### Task 10: 验证器契约同步（转达，无代码）

- [ ] **Step 1: 整理转达内容**

- `GET /user/page` 参数 `page/size` → `pageNum/pageSize`：验证器需检索全部用例中含 `/user/page?` 或 `&size=` 的调用并更新（已知 **TC-015 `user_page_query`** 用 `?page=1&size=5` → 改 `?pageNum=1&pageSize=5`；先前待审用户核对脚本 `?page=1&size=200&username=pending_audit` → 改 `pageNum=1&pageSize=200`）。
- 新增可选参数 `authStatus`（中文值：认证中/认证通过/认证失败/未认证）；`未认证` = 无 `usersinfo` 记录。
- `/user/page` OpenAPI 契约需刷新。
- 复测：更新 TC-015 后重跑；实名审核页分页/过滤/统计计数实跑（含造一条认证中记录验证通过/驳回按钮）。
